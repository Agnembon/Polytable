{
  "name": "polytable",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.2.21",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build --filter='./packages/*'",
    "clean": "bun x rimraf node_modules packages/*/node_modules"
  },
  "devDependencies": {
    "turbo": "^2.6.0",
    "typescript": "~5.9.3"
  }
}
